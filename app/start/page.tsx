'use client';

/**
 * /start — STAND onboarding entry point
 * StandOnboarding manages its own full-page layout including header and footer.
 */
import StandOnboarding from './components/StandOnboarding';

export default function StartPage() {
  return <StandOnboarding />;
}
